Use case: ui-mockup. The attached image is brand and style reference: agent-team violet/black faceted hexagon logo, Inter typography, rounded white cards with thin #e6e6ec borders, violet primary #7c3aed, light background #f7f7fa, ink text #0b0b10, muted text #62627a, success green #15803d, warning amber #b45309, destructive red #dc2626, Lucide line icons. Flat crisp production dashboard screenshot, precise readable English, restrained borders and very subtle shadows. No browser chrome. Generate ONE polished desktop UI prototype screenshot, exactly 1440x900 landscape.

Navigation: ONE single white left sidebar, 260px wide, full height, fine right divider. There is NO second navigation column and NO horizontal tab bar anywhere. At top of sidebar the hexagon logo and "agent-team" wordmark, and a small ghost icon button with the Lucide "panel-left-close" icon on the same row. Sidebar section label "PROJECTS" in small muted caps, then a project tree:
- "Recipe Box" EXPANDED, chevron down, small amber dot meaning waiting for a person. Under it, indented 16px, phase groups, each a small muted caps label with a status icon: "BUILD" with small green check, collapsed; "LAUNCH" expanded with a small amber dot; under LAUNCH, indented with a thin vertical guide line, items with Lucide icons: "Overview" (house), "Marketing" (megaphone), "Secrets" (key-round) with a small amber count badge "2". Then "OPERATE" collapsed, then "System" (gear) at the same indent as BUILD.
- "Dad Jokes" collapsed, small green dot meaning live.
- "Invoice API" collapsed, gray dot.
Then a divider, then "Incidents" and "Settings" at the bottom of the sidebar.
The sidebar item "Secrets" is selected with a pale violet rounded background and violet text and icon; no other item is highlighted.

Main content starts right of the sidebar with 32px gutters. Breadcrumb "Recipe Box / Launch / Secrets". Title "Recipe Box" with amber badge "Waiting for you".
Heading "Secrets", muted subtitle "Keys the app reads at deploy. The app lists them in .env.example. Values are encrypted and never shown again."

An amber-tinted alert card across the top with Lucide "key-round" icon: bold "Deploy waits for 2 secrets", text "The build uses fakes for these. Enter each value or skip it to go live." 

Main card titled "App secrets" with a muted right-aligned note "Live URL: https://brave-otter-42.trycloudflare.com" and a small copy icon. Inside, a list of four rows, each row: monospace name, a status chip, one or two lines of muted description, and actions on the right.
Row 1: "GOOGLE_CLIENT_ID" amber chip "Missing". Description "Google OAuth client ID. Create a Web client at console.cloud.google.com/apis/credentials. Redirect URI: <live URL>/api/auth/callback/google". Right side: a password-style input with placeholder "Paste value", a violet button "Save", and a ghost button "Skip".
Row 2: "GOOGLE_CLIENT_SECRET" amber chip "Missing". Description "Secret of the same Google OAuth client." Same input, "Save" and "Skip".
Row 3: "RESEND_API_KEY" green chip "Shared", small muted text "from Settings · updated 2 days ago". Description "Sends sign-in and digest emails." Right side ghost buttons "Override" and nothing else.
Row 4: "SENTRY_DSN" gray chip "Optional · skipped". Description "Error reports. The app works without it." Right side ghost button "Enter value".
Under the list, a muted row with a plus icon link "Add a secret the app does not declare".

Below, a small card titled "Apply to the live app" with text "Saved values reach the app on the next deploy." and an outline button with Lucide "rotate-cw" icon "Redeploy now".
