Use case: ui-mockup. The attached image is brand and style reference: agent-team violet/black faceted hexagon logo, Inter typography, rounded white cards with thin #e6e6ec borders, violet primary #7c3aed, light background #f7f7fa, ink text #0b0b10, muted text #62627a, success green #15803d, warning amber #b45309, destructive red #dc2626, Lucide line icons. Flat crisp production dashboard screenshot, precise readable English, restrained borders and very subtle shadows. No browser chrome. Generate ONE polished MOBILE UI prototype screenshot, exactly 390x844 portrait, of the same dashboard on a phone.

No sidebar is visible: a top bar with a hamburger menu icon, the small hexagon logo, and "Recipe Box" text. Breadcrumb "Launch / Secrets" in small muted text. Title "Secrets" with an amber badge "2 missing".
An amber-tinted alert card: bold "Deploy waits for 2 secrets", text "Enter each value or skip it to go live."
Then stacked cards, one per secret, full width with 16px side margins, no horizontal scroll:
Card 1: monospace "GOOGLE_CLIENT_ID" with amber chip "Missing", two lines of muted description "Google OAuth client ID. Redirect URI: <live URL>/api/auth/callback/google", a full-width password input "Paste value", and below it two buttons side by side, each 44px tall: violet "Save" and outline "Skip".
Card 2: "GOOGLE_CLIENT_SECRET" amber chip "Missing", description "Secret of the same Google OAuth client.", the same input and buttons, partly cut off at the bottom of the screen.
