Use case: ui-mockup. The attached image is brand and style reference: agent-team violet/black faceted hexagon logo, Inter typography, rounded white cards with thin #e6e6ec borders, violet primary #7c3aed, light background #f7f7fa, ink text #0b0b10, muted text #62627a, success green #15803d, warning amber #b45309, destructive red #dc2626, Lucide line icons. Flat crisp production dashboard screenshot, precise readable English, restrained borders and very subtle shadows. Generate ONE polished desktop UI prototype screenshot, exactly 1440x900 landscape, no browser chrome.

Navigation: ONE single white left sidebar, 256px wide, full height, fine right divider. At top the hexagon logo and "agent-team" wordmark, and a small ghost "panel-left-close" icon button on the same row. Section label "PROJECTS" in small muted caps with a small "+" button on the right. No projects are listed yet. At the bottom: "Incidents", "Settings", then a green dot with "Self-hosted" and muted "Connected", and a sun icon button.

Main content starts right of the sidebar with 32px gutters. The page is scrolled down: the top of the form card is cut off at the top edge. A single white form card, 740px wide. On its right, a 350px card "Your agent team" is out of view (scrolled away), so the right side is empty light background.

Inside the form card, from top:
- The end of the "Approval gates" grid: two columns of checkboxes. Left: "After concepts" (checked violet) with muted "Choose one of 3 logo and style directions. The branding follows your pick.", "After design" (checked) with muted "Review the UI design before the build.". Right: "After branding" (unchecked) with muted "Review the logo and screen images before design.", "After marketing" (unchecked) with muted "Review the launch images and copy before the plan.".
- Heading "Options". Four switch rows, violet when on: "Deploy when ready" on, muted "Runs the app on this server and shares a public preview link."; "Draw branding" on, muted "The illustrator draws a logo and desktop screens before the design step."; "Run sprints" on, muted "Once the app is live, a sprint runs every 7 days."; "Approve scope requests automatically" on.
- NEW FIELD, the focus of the image: label "Visual style". Below it a full-width select box, 36px tall, showing "Luxury / premium dark" with a chevron-down on the right.
- Under the select, a preview panel with a thin border and rounded corners, 16px padding: on the left a horizontal strip of five 24px square color swatches with no gaps, colors #0B0B0A, #151513, #EDE8DF, #C8A96A, #8A2F3B. On the right of the swatches: title "Luxury / premium dark" in medium weight, then muted text "Dark, restrained, image-led pages with thin serifs or wide caps, lots of air and very slow motion.", then muted text "Fits: Watches, jewelry, fashion, hotels, cars, spirits; Premium services (private banking, architecture)."
- Under the panel, muted helper text "Every concept direction uses this style, with its own logo, palette, and layout."
- A thin divider, then a violet button with a play icon "Start build", and muted text "Your agent team writes a spec, then gets to work."
NOT: no browser chrome, no second navigation column, no tab bar, no dropdown list opened, no lorem ipsum.
